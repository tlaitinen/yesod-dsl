        case getDefaultFilter filterParam_~{pn} defaultFilterJson "~{pn}" of
            Just localParam -> ~{maybeFrom} 
~{indent 8 (concatMap (implicitJoinExpr m ctx) joins)}
                where_ $ ~{hsExpr ctx expr}
            Nothing -> return ()
