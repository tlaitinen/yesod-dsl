        case getDefaultFilter defaultFilterJson "~{pn}" of
            Just localParam -> from $ \(~{selectVar} ~{concatMap joinDef joins}) -> do
~{indent 8 (concatMap (mapJoinExpr m ctx) joins)}
                where_ $ ~{hsExpr ctx expr}
            Nothing -> return ()
