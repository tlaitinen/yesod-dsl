        case getDefaultFilter defaultFilterJson "~{pn}" of
            Just localParam -> from $ \(~{intercalate ", " (map joinAlias joins)}) -> do
~{indent 8 (concatMap (implicitJoinExpr m ctx) joins)}
                where_ $ ~{hsExpr ctx expr}
            Nothing -> return ()
