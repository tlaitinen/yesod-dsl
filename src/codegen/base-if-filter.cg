        if hasDefaultFilter ~{pn} 
            then from $ \(~{selectVar} ~{concatMap joinDef joins}) -> do
~{concatMap (mapJoinExprIndent m ctx) joins}
                where_ $ ~{hsExpr ctx expr}
            else return ()
