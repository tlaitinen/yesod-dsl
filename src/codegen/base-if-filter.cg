        if hasDefaultFilter ~{pn} 
            then from $ \(~{selectVar} ~{concatMap joinDef joins}) -> do
~{concatMap (mapJoinExprIndent m ps) joins}
                where_ $ ~{hsExpr ps expr}
            else return ()
