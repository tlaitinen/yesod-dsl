    return $ object [ 
            "result" .= map (\~{intercalate " " params} -> object [
~{intercalate ",\n" fields}
            ]) result,
            "totalCount" .= (T.pack $ show (head (count)))
        ]
