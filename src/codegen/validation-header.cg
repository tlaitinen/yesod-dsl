checkResult :: forall (m :: * -> *). (Monad m) => Text -> m Bool -> m (Maybe Text)
checkResult msg f = do
   result <- f
   return $ if result then Nothing else (Just msg)

class Validatable a where
    validate :: forall m. (P.PersistQuery m, P.PersistEntityBackend a ~ P.PersistMonadBackend m) => a -> m [Text]

class ValidationFunction a where
