        if FS.hasDefaultFilter filterParam_~{pn} defaultFilterJson "~{pn}" 
            then ~{maybeFrom} 
~{indent 12 joinExprs}                 
                where_ $ ~{expr}
            else return ()
